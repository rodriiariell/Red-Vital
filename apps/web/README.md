# Web actual

Durante esta transición, el MVP web continúa en la raíz del repositorio para no modificar su interfaz ni su lógica. Este paquete solo aporta un servidor local independiente (`npm.cmd run dev:web`) que la sirve sin duplicar archivos.

La migración de la web a la API y su reubicación física se realizará en una etapa posterior.
